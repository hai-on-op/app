export type DynamicObject = {
    [key: string]: any
}

export type NumberMap = {
    [key: string]: number
}

export type IPaging = {
    from: number
    to: number
}

export type Vector2 = {
    x: number
    y: number
}
